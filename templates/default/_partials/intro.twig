{% if entry.intro|length %}
  <section class="intro-section" id="start">
    <div class="container">
      <div class="intro">
        {{ entry.intro }}
      </div>
    </div>
  </section>
{% endif %}